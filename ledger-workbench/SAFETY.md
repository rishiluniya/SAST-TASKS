# Safety

This repository contains deliberately unsafe code paths for controlled local analysis and evaluation. Do not deploy it, publish it as a starter application, connect it to real systems, or run it with real data or credentials.

Keep the service bound to localhost and use it only in a throwaway environment.
